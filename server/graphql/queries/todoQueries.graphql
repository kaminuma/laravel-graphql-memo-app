extend type Query {
    todos(
        completed: Boolean
        priority: Priority
        deadline_status: String
        sort_by: String
        sort_direction: String
        category_id: ID
    ): [Todo!]!
        @all
        @guard
        @whereAuth(relation: "user")
        @builder(method: "App\\Services\\TodoQueryBuilder@applyFilters")
}
