# GraphQL API エンドポイント
# 
# このファイルは環境変数設定のテンプレートです
# 必要に応じて .env.local にコピーして使用してください
# cp .env.example .env.local
#
# 各環境での設定例:
# - Docker環境: docker-compose.yml で環境変数が設定されているため、通常このファイルは使用しません
# - ローカル開発（Docker外でnpm startする場合）: .env.local にコピーして設定
# - 本番環境: .env.production を作成して本番URLを設定
#
# デフォルト値（ローカル開発用）:
REACT_APP_API_URL=http://localhost:8000/graphql
